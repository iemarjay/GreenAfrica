# GreenAfrica — On-chain Core (Hedera)

A single smart contract to register Reverse Vending Machines (RVMs), record verified PET (PEP) deposits, manage **Green Points**, handle **referrals**, and log **redemptions** (airtime/data). Built for a web3-abstracted UX: **only the backend admin/operator wallet writes** on behalf of devices and users.